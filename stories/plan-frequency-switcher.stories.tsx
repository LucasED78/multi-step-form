import { ComponentMeta } from '@storybook/react';
import PlanFrequencySwitcher from '../src/components/plan-frequency-switcher';

const PlanFrequencySwitcherMeta: ComponentMeta<typeof PlanFrequencySwitcher> = {
  title: 'PlanFrequencySwitcher',
  component: PlanFrequencySwitcher,
};

export const Default = {};

export default PlanFrequencySwitcherMeta;
